{
  "name": "rolldown-panic-8143",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@4.12.0",
  "scripts": {
    "build": "yarn workspaces foreach -A run build",
    "lint": "oxlint packages",
    "storybook": "storybook dev -p 6006",
    "build-storybook": "storybook build"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
